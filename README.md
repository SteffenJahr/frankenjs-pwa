# FrankenJS ServiceWorker demo

# Installation
To install this demo application you need NodeJs. In the main folder just execute `npm install`.

# Start
To start the web server just run `npm start` in the main folder.
